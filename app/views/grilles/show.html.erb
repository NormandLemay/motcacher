<p id="notice"><%= notice %></p>
<%= @grilles.display_name %>
<table border="2">
  <% (0...10).each do |y| %>
      <tr>
        <% (0...10).each do |x| %>
            <td width="30" align="center" height="30"><%= @grilles.get_val_x_y(x,y).upcase %></td>
        <% end %>
      </tr>
  <% end %>
</table>
<%= link_to 'Back', grilles_path %>
